# 本地开发环境配置
spring:
  mail:
    host: smtp.qq.com
    port: 465
    username: hanjor666@qq.com
    password: gubnpfervkukdfei
    protocol: smtps
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
  # 数据源配置
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/medical_model_platform?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    druid:
      initial-size: 5
      min-idle: 10
      max-active: 20
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1 FROM DUAL
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      pool-prepared-statements: true
      max-pool-prepared-statement-per-connection-size: 20

  # Redis配置 - Spring Boot 3.x新格式
  data:
    redis:
      host: localhost
      port: 6379
      password: redis123456
      database: 0
      timeout: 10s
      lettuce:
        pool:
          max-active: 200
          max-wait: -1ms
          max-idle: 10
          min-idle: 0

  # RabbitMQ
  rabbitmq:
    host: localhost               # RabbitMQ服务器地址 (Docker环境)
    port: 5672                    # RabbitMQ服务器端口
    username: admin               # RabbitMQ用户名
    password: admin123456         # RabbitMQ密码
    virtual-host: /               # 虚拟主机
    connection-timeout: 3000      # 连接超时时间（毫秒）
    requested-heartbeat: 60       # 心跳时间（秒）

# Sa-Token配置
sa-token:
  alone-redis:
    host: localhost
    port: 6379
    password: redis123456 
    database: 1

# 阿里云OSS配置
dromara:
  x-file-storage:
    default-platform: aliyun-oss-1
    aliyun-oss:
      - platform: aliyun-oss-1
        access-key: your-access-key
        secret-key: your-secret-key
        end-point: https://oss-cn-beijing.aliyuncs.com
        bucket-name: medical-platform-dev
        domain: https://medical-platform-dev.oss-cn-beijing.aliyuncs.com/
        base-path: dev/

# MyBatis-Plus配置
mybatis-plus:
  configuration:
    # 开启驼峰命名转换
    map-underscore-to-camel-case: true
    # 开启缓存
    cache-enabled: true
    # 开启延迟加载
    lazy-loading-enabled: true
    # 开启积极延迟加载
    aggressive-lazy-loading: false
  global-config:
    db-config:
      # 逻辑删除配置
      logic-delete-field: isDeleted
      logic-delete-value: 1
      logic-not-delete-value: 0
      # 主键策略
      id-type: ASSIGN_ID

# 日志配置
logging:
  level:
    com.okbug.platform: DEBUG
    com.okbug.platform.mapper: OFF

